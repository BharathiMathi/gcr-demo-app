name: React application

on:
  push:
      branches: [master]
  pull_request: 
      branches: [master]

jobs:
  build:

    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v1
      - name: Build @ Push Image
        run: |
          echo "${{secrets.DOCKERPW}}" | docker login -u "bhmat89" --password-stdin
          docker push bhmat89/git-action:v1


      
        
